#!/usr/bin/caspian

vibecode <<EOF
{
	"doc":    "test_cases_parsing_functions",
	"role":   "sibling of parse.casp holding transpiler fixtures for function / closure / method definitions and their parameter shapes (bare, rich-metadata, splat, kwsplat, @-sigil params, sugar `function &name` vs long-form `$name = function(...)`, etc.). Split out from parse.casp's `# Functions and closures` section wholesale — includes a handful of @-sigil / setat fixtures that had been clustered there but are technically about the @ sigil rather than functions; those can migrate to a dedicated at-sigil file later if the organizing principle calls for it. Same fixture format (boxed by `#===...` outer borders, sections `#--- code ---` / `#--- CaspJ ---` / `#--- CaspM ---` or `#--- CaspM refactor ---`), read by the same parse-extract module.",
	"status": "growing; the @-sigil fixtures near the top of this file might migrate to a dedicated at-sigil file if that organizing cut proves useful"
}
EOF

#====================================================================
# Functions and closures
#====================================================================


#====================================================================
# name: function sugar form (function &name) [ghi]
#

#--- Caspian -----------------------------------------------------------
function &welcome($name)
	return 'welcome, ' + $name
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: function sugar form (function &name)
#====================================================================


#====================================================================
# name: function with no params [ghi]
#

#--- Caspian -----------------------------------------------------------
function &greet()
	return 'hi'
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: function with no params
#====================================================================


#====================================================================
# name: function with multiple params [ghi]
#

#--- Caspian -----------------------------------------------------------
function &add($a, $b)
	return $a + $b
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: function with multiple params
#====================================================================


#====================================================================
# name: closure sugar form (closure &name) [ghi]
#

#--- Caspian -----------------------------------------------------------
closure &make_prefixer($name)
	return $prefix + $name
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: closure sugar form (closure &name)
#====================================================================


#====================================================================
# name: empty function body [ghi]
#

#--- Caspian -----------------------------------------------------------
function &noop()
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: empty function body
#====================================================================


#====================================================================
method &foo($bar, &gup)
end
#====================================================================


#====================================================================
# name: function with a rich-metadata param [ghi]
# `$name: {hash}` attaches constraints
#

#--- Caspian -----------------------------------------------------------
function &fetch($url: {class: 'string'})
	&get $url
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: function with a rich-metadata param
# `$name: {hash}` attaches constraints
#====================================================================


#====================================================================
# name: multiple params with rich metadata, mixed with a bare param [ghi]
#

#--- Caspian -----------------------------------------------------------
function &fetch($url: {class: :string}, $timeout: {class: :number, default: 30}, $opts)
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: multiple params with rich metadata, mixed with a bare param
#====================================================================


#====================================================================
# name: rich-metadata param on a closure [ghi]
#

#--- Caspian -----------------------------------------------------------
closure &make_greeter($prefix: {class: :string, default: 'hello, '})
	return $prefix + $name
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: rich-metadata param on a closure
#====================================================================


#====================================================================
# name: positional splat param [ghi]
# `*$args` collects extra positional args
#

#--- Caspian -----------------------------------------------------------
function &foo($x, *$args)
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: positional splat param
# `*$args` collects extra positional args
#====================================================================


#====================================================================
# name: kwargs splat param [ghi]
# `**$opts` collects extra keyword args
#

#--- Caspian -----------------------------------------------------------
function &foo(**$opts)
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: kwargs splat param
# `**$opts` collects extra keyword args
#====================================================================


#====================================================================
# name: mixed positional + splat + kwsplat params in one signature [ghi]
#

#--- Caspian -----------------------------------------------------------
function &foo($x, *$args, **$opts)
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: mixed positional + splat + kwsplat params in one signature
#====================================================================


#====================================================================
# name: splat param with rich metadata [ghi]
# `*$args: {class: :string}`
#

#--- Caspian -----------------------------------------------------------
function &foo(*$args: {class: :string})
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: splat param with rich metadata
# `*$args: {class: :string}`
#====================================================================


#====================================================================
# name: composite [ghi]
# long-form assignment with every rich-param feature (metadata / splat / kwsplat)
#

#--- Caspian -----------------------------------------------------------
$rich_params = function(
	$name,
	$rank: {optional: true, default: 'ensign'},
	$sent_title: {public: 'title'},
	&lazy_left,
	&lazy_right,
	*$args,
	**$opts,
	)
	return [$name, $rank, $sent_title, $rest, $opts]
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: composite
# long-form assignment with every rich-param feature (metadata / splat / kwsplat)
#====================================================================


#====================================================================
# name: long-form function assignment [ghi]
# `$name = function(...) end`
#

#--- Caspian -----------------------------------------------------------
$greet = function($name)
	return 'hello, ' + $name
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: long-form function assignment
# `$name = function(...) end`
#====================================================================

#====================================================================
# name: long-form closure assignment [ghi]
# `$name = closure(...) end`
#

#--- Caspian -----------------------------------------------------------
$make_prefixer = closure($name)
	return $prefix + $name
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: long-form closure assignment
# `$name = closure(...) end`
#====================================================================


#====================================================================
# name: function sugar form with a lazy param (`&read`) [ghi]
# `&` in param position declares laziness. Callee-side param spec
# carries `lazy: true`; caller-side is unchanged from the "args are
# always step-lists" rule (see atom-keys). Full mechanism in the
# sprint's lazy-mechanism doc.
#

#--- Caspian -----------------------------------------------------------
function &foo($idx, &read)
end
#--------------------------------------------------------------------

#--- CaspJ -----------------------------------------------------------

#--------------------------------------------------------------------

#
# name: function sugar form with a lazy param (`&read`)
#====================================================================
